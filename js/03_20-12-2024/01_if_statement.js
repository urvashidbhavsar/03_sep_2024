// compare two number is equal or not 

let num1 = 11, num2 = 12;

if (num1 === num2) {
    console.log(`${num1} and ${num2} numbers are equal`);
} else {
    console.log(`${num1} and ${num2} numbers are not equal`);
}